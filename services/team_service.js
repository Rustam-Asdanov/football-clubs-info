const Team = require("../models/Team");

const getTeams = () => {};

const getTeam = (id) => {};

const addTeam = (body) => {};

const deleteTeam = (id) => {};

const updateTeam = (id, body) => {};

module.exports = {
  getTeams,
  getTeam,
  addTeam,
  deleteTeam,
  updateTeam,
};
